## 1. 菜单数据接入

- [x] 1.1 新增前端 `site-menu` API 模块、菜单类型定义和后端 DTO 到前端目录模型的适配逻辑
- [x] 1.2 调整 `src/api/request.ts` 与 `vite.config.ts`，使前端开发环境默认可访问本地 `/api/site-menu/getMenu`

## 2. 目录界面改造

- [x] 2.1 在布局层实现共享菜单加载状态，统一向侧边栏和首页提供菜单数据
- [x] 2.2 重构 `AppLayout.tsx` 与 `HomePage.tsx`，用接口菜单替换静态 `toolSections` 渲染目录和卡片区
- [x] 2.3 为目录页补齐菜单加载态、失败态、空态，以及接口化数据下的入口打开行为

## 3. 验证与清理

- [x] 3.1 清理或收敛旧的静态目录数据依赖，保留仍需使用的非菜单常量
- [x] 3.2 运行前端构建或等效验证，确认目录接口接入后页面可正常渲染
