interface:
  display_name: "Frontend Design Guard"
  short_description: "Read design.md first, prefer the bundled shadcn theme, and default to restrained UI"
  default_prompt: "Use $frontend-design-guard to handle frontend work in this repository only after reading frontend-template/design.md first, then loading the bundled shadcn theme reference, preferring that theme, and defaulting to restrained minimal UI unless the user explicitly asks for stronger visual treatment."

policy:
  allow_implicit_invocation: true
